@model PrimerProyectoClubDeportivoPA2.Web.Data.Entities.AdditionalSkill

@{
    ViewData["Title"] = "Detalles Habilidad Extra";
}

<h1>Detalles</h1>

<div>
    <h4>Habilidad Extra</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Coach.User.FullName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Coach.User.FullName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Sport.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Sport.Name)
        </dd>
    </dl>
</div>
<div>
    @if (this.User.IsInRole("Admin") || this.User.IsInRole("Coach"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a>
    }


    <a asp-action="Index" class="btn btn-info">Regresar a la Lista</a>
    
</div>
